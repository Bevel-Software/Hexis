---
'@bevel-software/platform-core-backend': patch
---

Approving a proposed skill now puts it in the library straight away, instead of making it disappear for up to a minute first. The same fix makes a skill pushed from a developer's machine, or restored when a diverged workspace is reconciled, show up as promptly as one approved in the UI.
