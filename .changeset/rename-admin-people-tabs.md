---
"@bevel-software/platform-core-frontend": patch
---

Two admin settings tabs are renamed to say what they hold: "Roles & Members" is now "App roles", and "Groups" is now "Groups & Members". The page titles follow the tabs, and the hint shown when converting a role into a group points at the renamed page. Routes are unchanged, so existing links to `/roles-and-members` and `/directory-groups` keep working.
