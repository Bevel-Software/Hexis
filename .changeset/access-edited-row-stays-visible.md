---
'@bevel-software/platform-core-frontend': patch
---

In the Manage access sheet, a person whose row moves after a change stays on screen.

Revoking someone's edit on a folder where a parent still grants them read files them under "People invited to <parent>", which is collapsed, so the person being edited vanished and looked removed. The section the row lands in now opens itself after the write, from the row menu and from Remove alike; a row that stays on this folder leaves the sections as they were.
