---
'@bevel-software/platform-core-frontend': patch
---

The welcome page's ChatGPT option now shows numbered steps for ChatGPT's current settings, where the page is called Plugins. Each step also gives the old name (Apps & Connectors, with Developer Mode under Advanced), so the steps read right in either version of ChatGPT. "Add to ChatGPT" now opens ChatGPT's settings instead of the old Connectors page, and the steps continue from there. The External agent access page uses the same wording. The Other option is now called Other tools. It explains where the configuration goes: open the tool's settings, find MCP servers, choose add, and paste. It also shows the bare address with its own copy button, for tools that ask for a URL only.
